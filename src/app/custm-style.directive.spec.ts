import { CustmStyleDirective } from './custm-style.directive';

describe('CustmStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustmStyleDirective();
    expect(directive).toBeTruthy();
  });
});
